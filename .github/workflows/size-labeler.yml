name: Label PR by Size

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

jobs:
  size:
    permissions:
      contents: read
      pull-requests: write
    uses: anaverage-enri/.github/.github/workflows/size-labeler.yml@main

